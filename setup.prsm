~{
    var push = (item) => {
        this = [...this, item]
    }
    var log = str => {
        this.logs.push(str)
        return str
    }
    var logs = []
    pub console = {
        log: log,
        logs: logs
    }
}