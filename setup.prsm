{
    var push = (item) => {
        this = [...this, item]
    }
    var log = str => {
        this.logs = [...console.logs, str]
    }
    var logs = []
    pub console = {
        log: log,
        logs: logs
    }
}
