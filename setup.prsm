~{
    var push = (item) => {
        this = [...this, item]
    }
    var log = str => {
        logs.push(str)
    }
    var logs = []
    pub console = {
        log: log,
        logs: logs
    }
}
